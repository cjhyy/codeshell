# TODO

> 已完成项一律删除（记录在 git 历史与记忆里）。本文件只保留**未完成**的待办。
> 分区规则：**小 feature = 体量 M 及以下（M/S/XS），可单会话直接着手**；**大功能升级 = 体量 L**，需先方案设计再分阶段落地。
> 最近一次核对：2026-07-10（对照最近 80 条提交、nightly 汇总、重构/评审文档与当前代码：删除已完成的 Quick Chat、DriveAgentJobs 查询/取消、Node 24 Actions 及 rc.18 三条发版遗留；更新 engine/OAuth/命名/prompt cache、两条新增 feature 和大功能项的现状与锚点）。

---

## 小 feature（体量 M 及以下，现在可直接着手）

- **goal judge 改喂「最近 N 轮完整对话」替代单抽工具结果**（体量 S）｜背景：goal judge V1（分支 `feat/goal-judge-context`，未合并）为治「盲判」bug，从 TurnLoop 单独收集工具结果 + 12条/1600字符/head-tail 截断这套独立机制拼进 judge prompt。对照 CC `/goal`（Haiku 读 condition+完整对话）和 Codex Goals（主模型读整个 thread），两家都不单独抽工具结果——工具结果天然在对话里。｜核查结论（codex，只读）：**架构够得着，只是没接线**——stop-hook 当前只收窄 `HookContext`（`hooks/events.ts:27`/`:110` 仅 goal/finalText/turnCount），但 TurnLoop 触发时手里握着完整 `messages` + `Transcript`（`engine/turn-loop.ts:1005`/`:1034`/`:116`），`session/transcript.ts:149` `toMessages()` 可还原 assistant/tool_use/tool_result，`context/compaction.ts:931` 有现成按 API-round 分组 helper。｜修法（体量 S）：把私有 `GoalJudgeRuntimeContext`（`hooks/goal-stop-hook.ts:80`）从「toolResults+progress」改为携带「最近 N 个完整 API round 的对话切片」，调整 turn-loop.ts 生产、goal-stop-hook.ts prompt、engine.ts 接线及测试；删掉单抽工具结果那套；保留总字符/token 上限 + 敏感结果脱敏。不动公共 HookContext（动它才升 M 且有第三方 hook 工具输出泄露面）。｜决策：V1 已合并 main（merge `e4e1e959` / feat `c40e76c9`，含 2 blocker + 3 nit 修复，codex 最终复核 SHIP-with-nits，未 push）；此项作为后续「对齐 CC」的优化，不阻塞。
- **goal judge V1 最终复核遗留的 2 个非阻塞 nit**（体量 XS）｜背景：goal judge V1（已合并 `c40e76c9`）最终复核 SHIP-with-nits，新发现 2 个非阻塞 nit（详见 `docs/refactor/goal-judge-v1-final-review-codex.md`）：①**缓存键分隔符碰撞**——`goal-stop-hook.ts` 的 verdict 缓存 key 用简单分隔符拼多个字段，若某字段内容恰含该分隔符会造成不同输入拼出同一 key（误命中）。修法：用不可能出现在内容里的分隔符或对各段做长度前缀/JSON 序列化。②**JSON 转义后字符预算放大**——工具证据先按 code point 截到 1600/8000，再经 `JSON.stringify` 转义（引号/反斜杠/控制字符变 `\uXXXX`），实际进 prompt 的字符数会超预算。修法：按转义后长度计预算，或预留转义膨胀余量。｜决策：都不影响正确性/安全（blocker 已闭环），低优先顺手修。

- **拆 `engine.ts` 本体**（体量 M）｜锚点：`packages/core/src/engine/engine.ts:928`、`:1179`、`:1980`、`:2706`｜现状：`engine.ts` 仍 3627 行；`extractJSON`、`EngineConfig` 与约 200 行图片输入流程已提取（`39d7f218`），但门面仍混合 run 编排、subagent 回调、sandbox 解析、TurnLoop 装配和 runtime config 热刷新。修法：一次抽一块到独立模块，每块配 core 测试，保留 Engine 只做生命周期和装配。
- **MCP HTTP OAuth 登录/刷新闭环**（体量 M）｜锚点：`packages/core/src/credentials/types.ts:6`、`packages/core/src/tool-system/mcp-manager.ts:194`、`packages/desktop/src/renderer/credentials/LinkTab.tsx:40`、`packages/desktop/src/renderer/settings/McpSection.tsx:951`｜现状：OAuth credential、access/refresh/expiry 元数据、HTTP Bearer 注入、认证方式选择、状态展示和退出已落地（`660f320d`/`4caa8ded`）；但 LinkTab/MCP 设置里的登录与刷新按钮仍只提示 pending，过期 token 也明确不会自动刷新。修法：把登录/刷新按钮接到真实 host OAuth 流程，安全落盘并在临期/过期时刷新 token，补成功、失败、撤销和刷新回归测试。
- **命名收口 repo / workspace / project / cwd**（体量 M）｜锚点：`docs/nightly-2026-07-10/naming-consolidation-plan.md:4`、`packages/desktop/src/renderer/repos.ts:14`、`packages/core/src/types.ts:229`、`packages/core/src/settings/schema.ts:361`｜现状：概念表、目标命名和兼容迁移边界已完成，但文档明确「仅调研与迁移方案，不做代码改名」；renderer 仍用 `Repo`/legacy localStorage，core session 仍区分 cwd/workspace，settings 仍称 project scope。修法：按方案先引入兼容类型/adapter，再分批机械改 UI/state/bucket/settings 局部命名；保留旧 localStorage key、bucket 字节形状和 `state.json` 字段兼容。
- **Prompt cache 深化：粘性锁定、prefix hash 与破坏归因**（体量 M）｜锚点：`docs/todo/prompt-cache-optimization.md:91`、`packages/core/src/prompt/composer.ts:145`、`packages/core/src/engine/turn-loop.ts:429`、`packages/core/src/engine/engine.ts:2963`｜现状：OpenAI cached_tokens、Anthropic tools/history 断点、session cumulative usage、cacheable/volatile context 分离、compaction 剥离 dynamicContext 和 cache_read 暴跌告警均已落地（`13e0b826`/`b745b98b`）；当前告警仍只给通用 hint，尚不能指出 system/tools/config 哪个前缀变化，也未完成会话内动态开关的粘性审计。修法：作为性能专项记录 system/tools/config prefix hash，给暴跌告警做差异归因，并审计进入缓存键的动态开关是否需 session 锁定。
- **DriveAgent 展示区加「跳转到对应 Claude/Codex 会话」按钮**（体量 S）｜锚点：`packages/core/src/tool-system/builtin/background-jobs.ts:54`、`packages/core/src/tool-system/builtin/background-work.ts:101`、`packages/desktop/src/renderer/panels/BackgroundShellPanel.tsx:310`｜现状：DriveAgent job registry 已记录 `ccSessionId`/`cli`/`cwd`，但 UI-oriented `BackgroundWorkEntry` 没透传这些字段，展示区仍只有状态、文件和结果，无法一键进入底层 CLI 会话。修法：把外部 sessionId、CLI kind 和 cwd 贯通到 renderer，在 DriveAgent 任务卡片加按钮并打开/定位对应 Claude Code 或 Codex room。
- **浏览器面板地址栏支持右键「复制地址」**（体量 S）｜现状：内置浏览器面板顶部地址栏无法通过右键菜单直接复制当前 URL，只能手动选中文本再复制，不便分享/记录当前页地址。修法：①给地址栏元素加右键上下文菜单（或复制按钮），一键把当前 URL 写入剪贴板；②Cmd/Ctrl + 点击地址栏（或页内链接）用系统默认浏览器打开当前 URL（`shell.openExternal`），不走内置面板。确认 Electron webview/renderer 侧的 clipboard 写入与 openExternal 路径、权限。
- **session fork（带上下文分叉 / 选段压缩打包）**（体量 M，分两阶段）｜锚点：`packages/core/src/session/session-manager.ts:146`（未公开 fork）、protocol/session fork 协议暂缺、`packages/desktop/src/renderer/quickChatSession.ts:4`、`packages/desktop/src/renderer/App.tsx:3094`、技术方案 `docs/nightly-2026-07-10/session-fork-and-context-transfer-design.md`｜现状：quick-chat 创建的是空白 session（`qchat-*`，不带上下文），side chat 无意义——用户想要的是从当前会话分叉出去、带完整上下文的侧聊天。另外用户希望能选中一段对话、让模型压缩成 background context summary，再用这份摘要启动新 session 并复用当前 worktree（解决"冗長 debug 过程直接 copy-paste 太吵"的问题）。codex CLI 已有 `codex fork`（线性全量复制，无选段压缩，无 summary 打包），CodeShell 可做超集。修法分两阶段：①增强 `SessionManager.fork()` 按事件游标复制 transcript 快照，新增 `agent/forkSession` 协议与 `forkedFrom` lineage 字段，quick-chat 默认带当前上下文（可选空白）；② `MessageStream` 增加范围选择 + `summarizeContextPackage()`（复用 `/compact` 的 `buildSummarizationPrompt()`），用模型把选段压成约 1,500 token 背景摘要，以 `context_transfer` summary 事件存入新 session transcript，后续独立发展。两阶段共享 worktree 复用（已有 delete-guard），fork 不新建 worktree。文件锚点完整标注在方案文档。
- **手机遥控支持发图片**（体量 M）｜锚点：`packages/desktop/src/main/mobile-remote/types.ts:66`（`chat.send`）、`:110`（`room.send`）、`packages/desktop/src/mobile/components/Composer.tsx:7`、`packages/desktop/src/mobile/hooks/useRemoteApp.ts:773`、`packages/desktop/src/main/index.ts:833`/`:1148`｜现状：移动端协议与 `sendChat` 仍只带 `text`，Composer 也只有 textarea，无任何图片/附件字段，手机端根本传不了图片；旧锚点 `mobile-static.ts` 现在只负责静态资源服务，不是移动端 UI。**注意手机本地图片路径对桌面无意义（两台机器），只能传字节本身**。修法：①移动前端选图/拍照读成 bytes；②给 `chat.send`/`room.send` 加 `attachments`（小图走 base64 data URL，大图走单独 HTTP 上传端点，避免 WS 单帧塞大 base64）；③桌面收字节后落盘到 workspace（如 `.code-shell/uploads/xxx.png`）生成本地真实路径；④复用「统一输入附件总线」把落盘路径喂引擎。属于统一附件管线的一个入口，建议与其统一设计（见 memory `codeshell-input-attachment-pipeline-unified-plan-first`）。
- **[bug] 审查面板未暂存内容取错 workspace（不跟随 session worktree）**（体量 S）｜锚点：`packages/desktop/src/renderer/App.tsx:4123`、`:4138`、`:4161`、`packages/desktop/src/renderer/panels/PanelArea.tsx:452`、`packages/desktop/src/renderer/panels/PanelRegistry.ts:106`、`packages/desktop/src/renderer/panels/ReviewPanel.tsx:247`、`packages/desktop/src/renderer/diff/UnifiedDiffViewer.tsx:58`、`packages/desktop/src/main/desktop-services.ts:81`｜现状：Review 面板执行 `git diff` 用的 cwd 是项目登记路径 `panelRepo.path`（主仓库根），不是当前 session 的 `SessionWorkspace.root`。`App.tsx:4123` 解析 panelBucket 时只取 `repoId`、丢弃了 sessionId，随后 `:4138` 固定传 `cwd={panelRepo?.path}`。因此 session 指向某个 worktree 时，Review 仍在主仓库跑 git，展示的是主仓库的未暂存内容——与「browser 面板全局单例不按 session 隔离」同类。影响所有 git-backed 范围（未暂存/已暂存/全部未提交/最近提交/分支对比），只有「本轮改动」因走 turnDiff 快照不受影响。同一错误 cwd 也被 Files/Terminal 面板共享，值得顺带审计。修法：用 App 已在 `:4161` 算出的 engineSessionId 取 `getSessionWorkspace(engineSessionId, panelRepo.path)`（`session-manager.ts:258` / 桌面封装 `session-workspace-service.ts:163`），把 `workspace.root` 作为 ReviewPanel cwd 并监听 `workspace:changed` 按 sessionId 更新；或让 Review IPC 收 `sessionId + fallbackRepoPath`、由 main 侧统一解析 workspace root 再跑 git（更稳，避免 renderer 缓存漂移）。TopBar 的 WorkspaceIndicator 已正确用了这套（`WorkspaceIndicator.tsx:176/325`）可照抄。加回归测试：worktree/main 各放不同未暂存改动，断言 Review 请求最终用 worktree path、切回 main 后随事件更新。
- **子 agent 定义支持 sandbox / mcp（skills 已支持）**（体量 M）｜锚点：`packages/core/src/agent/agent-definition.ts:4`（`AgentDefinition` 接口）、`:43`（`RawFrontmatter`）、`:76`（`parseAgentDefinition`）、`:117`（`serializeAgentDefinition`）、`packages/core/src/tool-system/builtin/agent.ts`（spawn 时装配子 Engine）｜现状：`AgentDefinition` 已支持 `model`/`maxTurns`/`tools`/`skills`（skills allowlist 已落地，见 `agent-definition.skills.test.ts`），但**没有 `sandbox` 和 `mcp` 字段**——子 agent 无法各自声明沙箱策略或 MCP server 子集，只能继承父 session 全量。对照 Codex custom agent（`~/.codex/agents/*.toml` 支持 `sandbox_mode` + `mcp_servers`，omit 则继承父）。修法：①`AgentDefinition` 加 `sandbox?`（沙箱/审批策略枚举，undefined→继承父）和 `mcp?: string[]`（MCP server allowlist，undefined→继承父全量，`[]`→无 MCP，与 skills 同构语义）；②`RawFrontmatter` + `parseAgentDefinition`/`serializeAgentDefinition` 解析/序列化这两个字段（复用 `normalizeNameList` 处理 mcp 列表）；③spawn 子 Engine 时把 sandbox 策略接到现有 sandbox 解析、把 mcp allowlist 接到 MCP manager 的 server 过滤；④补 parse/serialize/inherit/empty-array 语义测试 + 一条 spawn 端到端断言子只见白名单内 MCP server。注意：先核实 core 现有沙箱策略的枚举/类型（engine.ts sandbox 解析处）再定 `sandbox` 字段形状，避免和现有 permission mode 重名冲突。
- **父 agent ↔ 运行中子 agent 双向通知/指令（不做兄弟 mesh/Team）**（体量 M，先出设计稿）｜锚点：`packages/core/src/tool-system/builtin/agent-notifications.ts`（`NotificationQueue`+`agentNotificationBus`，现为单向完成通知）、`packages/core/src/tool-system/builtin/agent.ts`（`AgentSendInput` 当前拒绝 running child）、`agent-heartbeat.ts`（progress 基座）、`asyncAgentRegistry`（`agent-registry.ts`）、调研报告 `docs/research/multi-session-agent-communication-landscape.md:230`（第 6 节 Phase 0）｜决策：**只做 tree 内「大脑↔子」双向控制面，明确不做兄弟 session mesh、不做显式 Team**（对齐 Codex/Claude：两家都不开放无差别 sibling mesh；卡密sama 确认「大脑通知一下就够」）。现状：子完成→父 idle drain 是单向；`AgentSendInput` 拒绝运行中子（怕第二个 Engine resume 同 transcript 并发写坏）。修法（复用现有基建，不建平行总线）：①把「完成通知」归一为 `NotificationQueue` 上的一种 envelope（加 `kind`/`delivery`/`sequence`/`correlationId` 等最小字段），progress/direction/result 共用同一 per-session bucket + idle drain + bus fan-out；②让**现有 child Engine** 拥有 inbound queue：父 `AgentSendInput(agentId, prompt, delivery)` 只路由 envelope 不新建 Engine，`next-safe-point` 在 model response/tool batch 边界合并、`interrupt-and-redrive` 先 abort 当前 batch 再开新 turn，返回 ACK(`queued|delivered|interrupted|rejected`)；③把 heartbeat 扩成结构化 progress（lastTool/phase/tokens/1 行 summary），父按需读、不流式复制子完整 transcript；④agent-origin direction 绝不当作用户 consent，permission 永远只认用户/既有 policy。**范围红线（本期只做 Phase 0）**：不碰兄弟无差别 mesh、不碰跨顶层 session bus、不做 TaskBoard/Budget。**但 envelope 必须为未来「显式 Team」预留扩展位**——`from`/`to` 用通用 `{sessionId, agentId?, authority}` 而非写死 parent/child，并预留可选 `teamId`/`correlationId`，使 tree 只是「无 teamId 的退化形态」。**Team 不是永久砍掉，而是数字人阶段的 Phase 2**（数字人 = 长期 Profile 身份，天然需要「一个数字人 lead 指挥多 worker、成员固定身份 + 单播 mailbox + ACL」，正是报告 §6.4 的显式 Team；见 memory `codeshell-multi-session-communication-direction`）。届时只在同一套 mailbox 上加 team 成员表 + 单播 ACL，不推倒 Phase 0。建议先走 brainstorming→writing-plans 出一份 Phase 0 设计稿再实现，设计稿须显式论证「envelope 如何平滑演进到 Team」。

---

## rc.18 发版遗留（非阻塞项，发 0.7.0 正式版前处理）

> 本次核对后暂无未完成项。

---

## 大功能升级（体量 L，需方案设计 + 分阶段落地）

- **core 通用化 + 插件面板**（体量 L）｜锚点：`packages/core/src/preset/index.ts:36`、`packages/desktop/src/renderer/panels/PanelRegistry.ts:59`、`packages/core/src/plugins/installer/types.ts:4`｜现状：Sleep/CronList 已先行集中 builtin 元数据，Desktop 内置面板也已收敛到 `PanelRegistry`（`15b2b432`/`0d16f9f9`）；但默认 preset/tool 清单仍大段硬编码，registry 仍由固定 `PanelTab` 映射约束，plugin manifest 仍无 `panels`。修法：继续用工具元数据派生 preset，把 PanelRegistry 扩展为可注册边界，再落 manifest `panels` + `csplugin://` 沙箱 host + scoped bridge。
- **聊天软件 channel / IM gateway（含隧道地址回推微信）**（体量 L）｜锚点：`docs/todo/im-gateway-remote-orchestration.md:36`、`packages/desktop/src/main/mobile-remote/tunnel-manager.ts:94`、`packages/desktop/src/main/mobile-remote/remote-host-manager.ts:270`｜现状：mobile tunnel 与 pairing URL 已在 Electron main 内部可生成，但无 IM/webhook/bot adapter、无 gateway 进程、无微信/Telegram 回推。修法：先做 `/open /close /status` gateway MVP，复用 tunnel/pairing/passcode，再接可插拔 channel。
- **Workspace / Profile / 数字人**（体量 L）｜锚点：`docs/todo/workspace-profile-讨论稿.md:86`、`packages/core/src/settings/schema.ts:25`、`packages/core/src/prompt/composer.ts:270`｜现状：已有 capability overlay、preset、plugin、普通 `userProfile`，但没有 `WorkspaceProfile` schema、激活/切换事务、主指令注入和数字人记忆层。修法：先落全局 profile 库 + activeProfile 记录 + capability 批量写入，再接 mainInstruction、portable memory、Team Board。
- **Workspace 数据源绑定**（体量 L）｜锚点：`docs/nightly-2026-07-10/large-feature-breakdown.md:139`、`packages/core/src/settings/schema.ts:25`、`packages/desktop/src/renderer/credentials/LinkTab.tsx:9`｜现状：Connections/Link 已有固定 catalog、OAuth 凭证状态和退出，不再是完全静态壳；但仍没有独立的 SourceDefinition / WorkspaceSourceBinding、外部源连接状态、Figma/issue/云盘 scope 分配和按 workspace/profile 求交的读取面。修法：设计数据源 schema、权限/凭证绑定和按 workspace/profile 注入的读取面。
- **worktree session 隔离深化（外部 agent 自动隔离）**（体量 L）｜锚点：`docs/todo/worktree-session-isolation-research.md:331`、`packages/core/src/tool-system/builtin/drive-claude-code.ts:59`、`:263`、`packages/core/src/cc-orchestrator/external-agent-session-store.ts:17`｜现状：主 session workspace pointer、下一轮 cwd、DriveAgent resume cwd 绑定已落地；DriveAgent schema 仍无 isolation/baseRef/include，成功绑定也只记录 cli/sessionId/cwd，尚未建立自动 per-run worktree 生命周期。剩余是并行自动隔离、完成后保留/清理提示、`.worktreeinclude`/baseRef/cleanup。修法：给 DriveAgent 增 isolation 策略和生命周期 UI，再扩展 include/baseRef/lock。
- **工程质量 P7：builtin 工具集成 harness / Electron e2e / CI 覆盖率**（体量 L）｜锚点：`packages/core/src/tool-system/builtin/tool-coverage.test.ts:95`、`package.json:17`、`.github/workflows/ci.yml:45`、`packages/desktop/scripts/smoke-panels.mjs:1`｜现状：builtin 59 项覆盖矩阵已落地（44 covered / 15 skip，且明确是 informational）；根脚本仍无 coverage/e2e/smoke，CI 仍跑 targeted tests，desktop 只有 Playwright 依赖和一次性 smoke script，未成稳定集成/e2e 基座。修法：共享 ToolRegistry fake-context harness、mock provider smoke、Electron `_electron` harness、CI xvfb/e2e 分层，最后加覆盖率目标。
- **架构债 P1/P2（不含上方拆 engine，含 Goal 持久化）**（体量 L）｜锚点：`docs/todo/architecture-debt.md:24`、`docs/refactor/goal-persistence-final-design.md:3`、`packages/core/src/index.ts:281`、`packages/core/src/tool-system/builtin/index.ts:693`、`packages/core/src/types.ts:299`｜现状：index.ts 仍暴露大量 Arena 面，arena 仍默认 builtin，renderer `App.tsx` 仍 4286 行；main-mediated credential access + SafeStorageCipher 已落地，不再是本项欠账。Goal 强制终止主路径已修，但最终持久化稿明确仍是「仅供后续 TDD 实现」，当前仍为 `activeGoal + goalTerminal` 双字段和 whole-state `saveState` 合并。修法：拆 public/internal export、arena 可选注册/移包、拆 renderer App、收敛 state 单例；按最终设计用 versioned `goalLifecycle` + 领域更新 API 收口 Goal 持久化，并补 cron DST/闰边界与文档措辞。

---

## 约束边界（明确不做）

- **quick-chat 不做 Pi 式 parent 指针树状 session**：快聊是用完即走短对话；需要合并时用 fork/复制派生，不引入树状会话模型。
- **IM gateway MVP 不做编排大脑 / IM 内富交互审批 / 多租户**：gateway 只做通道、隧道生命周期和入口回推；高阶跨 session 指挥留给未来 assistant 主体。
- **WorkspaceProfile MVP 不做同一 workspace 同时激活多个 Profile**：当前决策是同一 workspace 一个 active Profile，可切换但不并存；项目专属定制仍放 `CLAUDE.md`/项目指令。
