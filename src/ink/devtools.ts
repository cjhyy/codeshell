/**
 * Devtools connection stub — conditionally imported by the reconciler
 * when NODE_ENV === 'development'. No-op in production.
 */

// This module intentionally does nothing in the codeshell build.
// In Claude Code, it connects to react-devtools-core.
export {};
