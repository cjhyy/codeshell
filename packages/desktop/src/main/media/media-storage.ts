/** Compatibility names preserve the original scope hash and on-disk layout. */
export {
  MAX_MEDIA_JSON_BYTES,
  normalizeMediaScope,
  mediaScopeKey,
  prepareMediaRoot,
  mediaDirectory,
  cloneMediaJson,
  readMediaJson,
  writeMediaJson,
} from "@cjhyy/code-shell-server/panels";
