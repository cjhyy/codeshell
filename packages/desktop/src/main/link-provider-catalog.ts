/** Compatibility facade; both hosts use the same validated catalog. */
export {
  composeLinkProviderCatalog,
  listDesktopLinkProviders,
} from "@cjhyy/code-shell-server/links";
export type { DesktopLinkProviderView } from "@cjhyy/code-shell-server/links";
