# Fix Batch 9 - 2026-07-08

Scope: bounded, low-risk fixes from `docs/archive/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. Batches 1-8 plus the verification fix already
handled the earlier CDP/core/TUI/desktop/infra clusters, so this batch selected
the remaining localized TUI workflow items with focused regressions and one
local commit per finding.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| Sub-agent thinking mixes into main spinner | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/tui/src/ui/App.tsx:576` | `cc25bc3187e0a5931e9a71c6cf1ae8613dc1e237` | `packages/tui/src/ui/App.stream-routing.test.ts`: main-agent thinking deltas are accepted while sub-agent thinking deltas are dropped from the main feed | Failing regression observed, then `bun test packages/tui/src/ui/App.stream-routing.test.ts` passed twice before commit. |
| New-message pill/divider has no scroll-away source | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/tui/src/ui/components/FullscreenLayout.tsx:170` | `a49c1b72a65871d72d55d4c7a173430deadf1ec1` | `packages/tui/src/ui/components/VirtualMessageList.test.tsx`: imperative list scroll breaks sticky-bottom and calls the scroll-away callback | Failing regression observed, then `bun test packages/tui/src/ui/components/VirtualMessageList.test.tsx` passed twice before commit. |
| Background-agent notifications can be dropped | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/tui/src/ui/App.tsx:1441` | `2e7111c50b6d73333cb321ef5832722d3b0e9210` | `packages/tui/src/ui/App.notifications.test.ts`: notification drain is blocked when the synchronous query guard is already busy despite a stale React idle snapshot | Failing regression observed, then `bun test packages/tui/src/ui/App.notifications.test.ts` passed twice before commit. |

Final focused verification:

`bun test packages/tui/src/ui/App.stream-routing.test.ts packages/tui/src/ui/components/VirtualMessageList.test.tsx packages/tui/src/ui/App.notifications.test.ts`

Result: `4 pass`, `0 fail`.

## Skipped / Deferred

- Already-fixed findings from Batches 1-8 and verification commit `5ece9d28`
  were skipped to avoid conflicts.
- Remaining red findings are not low-risk localized fixes: credential plaintext
  tool-output persistence needs a sensitive-result/redaction contract across
  tool execution, transcripts, UI streams, logs, and future prompts; Electron
  predist lockfile materialization needs packaging/install workflow redesign;
  plugin executable trust gates need explicit manifest/trust policy for hooks
  and MCP stdio commands.
- Remaining desktop browser/session findings are broader cross-surface work:
  global active guest targeting, browser open/reload panel scoping, sensitive
  browser approval, desktop credential safe-storage migration, and popout anchor
  bucket ownership all require main/preload/renderer ownership or DOM-style
  harness coverage.
- Explicitly out-of-scope items were skipped by instruction, including engine
  split, arena extraction, steer step-boundary changes, compaction token
  estimation rewrite, credentials partition redesign, MCP login-shell PATH root
  fix, `NEEDS-DEEPER-VERIFICATION`, `FALSE-POSITIVE / BY-DESIGN`, and desktop
  `.tsx` work requiring a DOM harness.

Conclusion: after the three batch 9 commits, the remaining CONFIRMED-LIVE
backlog is large/architectural, needs deeper verification, explicitly out of
scope, or requires broader UI/desktop harness work. Low-risk self-contained
fixes are exhausted.
