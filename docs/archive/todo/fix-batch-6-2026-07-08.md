# Fix Batch 6 - 2026-07-08

Scope: bounded, low-risk fixes from `docs/archive/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. Batches 1-5 already handled 23 findings, so this
batch selected remaining localized infra/prompt/TUI items with focused
regressions and one local commit per finding.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| npm publish not gated by version check | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `.github/workflows/release.yml:190` | `7f5942c87b55d6f9198b7dad19ea2bbe1de6e095` | `packages/core/src/release-workflow.test.ts`: `npm publish is gated by tag/package version verification for every release package` | Failing regression observed, then `bun test packages/core/src/release-workflow.test.ts` passed twice before commit. |
| Release jobs inherit `contents: write` | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `.github/workflows/release.yml:22` | `949cb6fb51c3020ff4c8ef3ce4fe87ffb333e9f9` | `packages/core/src/release-workflow.test.ts`: `release write permissions are isolated to the GitHub Release job` | Failing regression observed, then `bun test packages/core/src/release-workflow.test.ts` passed twice before commit. |
| CI does not run lint; guardrails are bypassable | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `.github/workflows/ci.yml:16` and `eslint.config.js:97` | `eb568522ae4743a2896b20f9b09ff19969d10381` | `packages/core/src/ci-workflow.test.ts`: guard job installs and runs lint; `tests/eslint-boundary-guard.test.ts`: dynamic/relative boundary imports are rejected | Failing regressions observed, then focused tests passed before commit. `bun run lint` also passed after preserving caught-error causes in three localized wrappers. |
| `<system-reminder>` wording is spoofable | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/prompt/sections/base.md:10` | `e1de20fd10d7614666e5faa8d95dad9925e8fc31` | `packages/core/src/prompt/composer-system-reminder.test.ts`: loaded system prompt treats system-looking tags inside untrusted content as data | Failing regression observed, then `bun test packages/core/src/prompt/composer-system-reminder.test.ts` passed twice before commit. |
| REPL bypasses max-context helper | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/tui/src/cli/commands/repl.ts:144` | `8a0a2c7c1ef07b36ff3707491e9867d73e464ecb` | `packages/tui/src/cli/commands/max-context-tokens.test.ts`: REPL uses the shared resolver instead of duplicating fallback logic | Failing regression observed, then `bun test packages/tui/src/cli/commands/max-context-tokens.test.ts` passed twice before commit. |

Final focused verification:

`bun test packages/core/src/release-workflow.test.ts packages/core/src/ci-workflow.test.ts tests/eslint-boundary-guard.test.ts packages/core/src/prompt/composer-system-reminder.test.ts packages/tui/src/cli/commands/max-context-tokens.test.ts && bun run lint`

Result: `9 pass`, `0 fail`; `bun run lint` exited `0` with existing warnings only.

## Skipped / Deferred

- Batch cap reached at five fixes.
- Already-fixed findings from Batches 1-5 were skipped to avoid conflicts.
- Remaining red findings were deferred because they still require broader
  policy, packaging, executable-trust, or sensitive-result design:
  credential plaintext tool-output persistence, Electron predist lockfile
  materialization, and plugin executable trust gates.
- Explicitly out-of-scope items were skipped by instruction, including engine
  split, arena extraction, steer step-boundary changes, compaction
  token-estimation rewrite, credentials partition mismatch, MCP login-shell PATH
  root fix, `NEEDS-DEEPER-VERIFICATION`, and `FALSE-POSITIVE / BY-DESIGN`
  entries.
- Desktop/TUI component findings needing a DOM or mounted-App harness were
  deferred, including browser-panel/session scoping, sensitive browser approval,
  provider key masking, async slash-command failure rendering, notification
  routing, markdown width cache rendering, and compact tool-result rendering.
