# Fix Batch 1 - 2026-07-08

Scope: bounded, low-risk fixes from `docs/archive/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. This batch selected a localized CDP cluster so each
finding could be covered by focused TDD in one affected test file.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| CDP `clickNode` falls back to JS `.click()` | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/cdp/src/driver.ts:130` | `da84f435dda47bd6bc8482ae89da8dd1f85bde8d` | `packages/cdp/src/driver.test.ts`: `CdpActionsDriver.clickNode > fails when the node has no layout box instead of using JS .click()` | Failing regression observed, then `bun test packages/cdp/src/driver.test.ts` passed twice before commit. |
| CDP click target can be outside viewport | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/cdp/src/driver.ts:119` | `8ef89609a04e327b49f63d10ffa087b42661b34b` | `packages/cdp/src/driver.test.ts`: `clicks inside the visible viewport intersection`; `fails when the node has no visible viewport intersection` | Failing regressions observed, then `bun test packages/cdp/src/driver.test.ts` passed twice before commit. |
| CDP `fetchImageData` interpolates `maxDim` | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/cdp/src/driver.ts:295` | `be7a393bd68dde4b86ac1d5aa2d81f2fb99c511b` | `packages/cdp/src/driver.test.ts`: `CdpActionsDriver.fetchImageData > sanitizes maxDim before interpolating it into page JavaScript` | Failing regression observed, then `bun test packages/cdp/src/driver.test.ts` passed twice before commit. |
| CDP video refs are not returned | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/cdp/src/driver.ts:646` | `32ab8d10510e6939f3737513afe3d4e5ea68d225` | `packages/cdp/src/driver.test.ts`: `buildExtractScript > includes link, image, and video collection + ref tagging` extended to assert video refs | Failing regression observed, then `bun test packages/cdp/src/driver.test.ts` passed twice before commit. |
| Missing image/video refs are not stale | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/cdp/src/driver.ts:302` | `bf66737a3d817b2f5b201d6da59fd06e09c4ab26` | `packages/cdp/src/driver.test.ts`: `CdpActionsDriver.fetchImageData > reports missing ref` extended to assert `staleRef` | Failing regression observed, then `bun test packages/cdp/src/driver.test.ts` passed twice before commit. |

Final focused verification: `bun test packages/cdp/src/driver.test.ts` passed after all
five commits with `31 pass`, `0 fail`.

## Skipped / Deferred

- Red findings deferred as outside this bounded low-risk pass:
  - Project settings `__proto__` smuggling: requires recursive settings sanitization and merge-policy hardening across trusted and untrusted config paths.
  - Credential values persisted as tool output: requires a cross-cutting sensitive-result/redaction design across transcripts, logs, UI streams, and future prompts.
  - npm publish not gated by version check: workflow/release policy change without an existing focused TypeScript regression target under `tests/` or `packages/*/src/**`.
  - Electron predist resolves deps outside lockfile: packaging/materialization workflow change with release-supply-chain implications.
  - Plugin executable surfaces lack trust gate: requires plugin trust policy, manifest diffing, and executable-surface enablement design.
- `NEEDS-DEEPER-VERIFICATION` entries were skipped by instruction:
  - `@cjhyy/code-shell-cdp` publishability is ambiguous.
  - Printable CDP `pressKey()` events may omit text payloads.
- `FALSE-POSITIVE / BY-DESIGN` entry skipped by instruction:
  - Custom ESLint rules are no-op stubs.
- Remaining `CONFIRMED-LIVE` findings were deferred because the five-fix batch cap was reached or because they involve desktop/TUI/core architecture, cross-package policy, release infrastructure, or broader state-management changes.
