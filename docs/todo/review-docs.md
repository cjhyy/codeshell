# Documentation Review Findings

## P0 Wrong / Misleading

- [ ] [P0] `README.md:194`, `README.zh-CN.md:190`, `packages/tui/README.md:69`, `docs/core-deep-dive/04-llm-model-layer.md:83`, `docs/core-deep-dive/v2-04-model-context-memory-deep-dive.md:122` — stream idle watchdog is documented as opt-in/disabled by default, but `packages/core/src/llm/stream-watchdog.ts:96` enables it unless `CODESHELL_ENABLE_STREAM_WATCHDOG=0` — update all user-facing docs to say it is on by default and document the opt-out env value.
- [ ] [P0] `README.md:124`, `README.zh-CN.md:122` — built-in tool list is materially stale: it omits current default-visible tools such as `ApplyPatch`, `TodoWrite`, `AskUserQuestion`, browser tools, background shell tools, memory/credential/goal tools, and external-agent tools, while listing old/nonexistent `TaskCreate`/`TaskUpdate`/`TaskList`/`TaskGet`/`TaskOutput` and `SendMessage` — regenerate this section from `packages/core/src/tool-system/builtin/index.ts` and `packages/core/src/preset/index.ts`.

## P1 Stale / Missing

- [ ] [P1] `README.md:27`, `README.zh-CN.md:27` — release status says `0.6.0-rc.10`, while all package manifests are `0.6.0-rc.12` — update the displayed status or replace it with a non-versioned beta-readiness note.
- [ ] [P1] `docs/architecture/00-overview.md:14` — architecture docs call the exported core `VERSION` (`0.6.0-rc.8`) the current public version even though manifests are `0.6.0-rc.12` — either align the source constant and docs, or state that `package.json` is canonical and avoid documenting the stale constant as the public version.
- [ ] [P1] `README.md:250`, `README.zh-CN.md:246` — development flow lists `bun run typecheck` as a normal check, but it currently exits nonzero on `packages/core/src/tool-system/builtin/drive-claude-code.test.ts:158` and `CODESHELL.md:50` says typecheck is not a clean gate — add the caveat to the public READMEs or fix the pre-existing type error.
- [ ] [P1] `README.md:253`, `README.zh-CN.md:249`, `packages/desktop/CLAUDE.md:27` — desktop verification says to run only `bunx tsc --noEmit` and `bun run build:renderer`; the package script actually has `bun run typecheck` for main/preload plus `tsconfig.mobile.json`, and `bun run build` builds main, preload, renderer, and mobile — replace the commands with `cd packages/desktop && bun run typecheck && bun run build` or explicitly call out when renderer-only checks are enough.
- [ ] [P1] `packages/tui/README.md:42` — README says plugin/skill management is not exposed as top-level CLI subcommands, but `packages/tui/src/cli/main.ts:166` registers `code-shell plugin` and `packages/tui/src/cli/commands/plugin.ts:12` exposes install/list/update/uninstall — document the `plugin` command and clarify which skill operations remain REPL/desktop-only.
- [ ] [P1] `CODESHELL.md:42` — repo guidance says `@/*` maps to `src/*` in each package via tsconfig/tsup, but only desktop defines `@/*`, it maps to `src/renderer/*`, and there is no `tsup.config.*` in the repo — rewrite this as a desktop-renderer-only alias note.
- [ ] [P1] `CODESHELL.md:53` — renderer guidance says `window.codeShell.*`, but the preload exposes `window.codeshell` lowercase — fix the capitalization so assistants do not write against a nonexistent API.
- [ ] [P1] `CODESHELL.md:71` — known-debt note says plugin `SessionStart` hooks are not wired, but `Engine.run()` emits `on_session_start` with `source` at `packages/core/src/engine/engine.ts:1540` and injects returned hook messages at `engine.ts:1836` — remove the stale debt item or replace it with a current, verified plugin-start limitation.
- [ ] [P1] `TODO.md:27` — TODO says `SessionStart` matcher `source` is not passed and fires too broadly, but `engine.ts:1544` now passes `source: "resume" | "startup"` and `loadPluginHooks.ts:121` consumes it — mark this item fixed or re-audit for a remaining issue.
- [ ] [P1] `docs/todo/README.md:4`, `docs/todo/README.md:39`, `docs/todo/roadmap.md:9` — links point to missing `../architecture/feature-inventory.md` and roadmap still says 149 capabilities; the actual file is `docs/architecture/11-feature-inventory.md` with 179 capabilities — fix the link target and count.
- [ ] [P1] `docs/archive/architecture/README.md:71` — archived index links to missing `../../architecture/feature-inventory.md`, says 149 capabilities, and still describes a dedicated desktop architecture chapter as missing even though `docs/architecture/10-desktop-and-mobile.md` exists — update the archive banner/link or point readers to the current architecture index.
- [ ] [P1] `docs/archive/architecture/01-system-overview.md:38` — archived architecture pages contain many broken links to the old pre-monorepo `src/...` layout (`../../src/cli`, `../../src/engine`, etc.) — either repair representative anchors to `packages/core`/`packages/tui`/`packages/desktop`, or add an explicit “links are historical and may be broken” archive warning.
- [ ] [P1] `docs/todo/roadmap.md:14`, `docs/todo/roadmap.md:85` — roadmap says `TodoWrite`/`TodoRead` are implemented, but code only has `TodoWrite` (`packages/core/src/tool-system/builtin/task.ts:50`) and no `TodoRead` symbol — update the roadmap to say task state is recovered from `TodoWrite` snapshots.
- [ ] [P1] `docs/core-deep-dive/04-llm-model-layer.md:90` — v1 deep dive says `scripts/sync-models.ts` is a build dependency, but root `package.json` build does not run it and `CODESHELL.md:55` explicitly says it is manual — change this to “manual snapshot refresh, not part of build.”

## P2 Style / Clarity

- [ ] [P2] `docs/core-deep-dive/02-engine-turn-loop.md:85`, `docs/core-deep-dive/v2-02-engine-turn-loop-deep-dive.md:215` — context-compaction prose still uses old `TaskUpdate` as a state-bearing example even though current task tracking is `TodoWrite` — replace `TaskUpdate` with `TodoWrite` or use a generic “state-bearing orchestration tools” phrase.
- [ ] [P2] `bench/README.md:3`, `bench/README.md:24` — bench docs refer to `src/render` and `src/render/README.md`, but the renderer lives under `packages/tui/src/render/README.md` — update paths to the monorepo layout.
- [ ] [P2] `CODESHELL.md:62` — guidance mentions `TODO-week.md` for the current week’s plan, but no such file exists — remove the reference or create/link the intended current-plan document.
- [ ] [P2] root `CLAUDE.md` / `AGENTS.md` — the requested doc surface includes these compatibility files, but neither exists at the repo root; tools that only look for their native instruction filename will miss `CODESHELL.md` — consider adding short stubs that point to `CODESHELL.md`, or document that root compatibility files are intentionally absent.
