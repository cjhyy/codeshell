# Fix Batch 2 - 2026-07-08

Scope: bounded non-CDP fixes from `docs/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. Batch 1 already handled the CDP cluster, so this
batch stayed in core/infra paths with focused regressions and one commit per
finding.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| `InjectCredential` ignores settings scope | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/credentials/inject-credential-tool.ts:81` | `a8158866110fb004f5c8d12855a819e5ea802ef4` | `packages/core/src/credentials/inject-credential-tool.test.ts`: project-scoped injection cannot resolve user cookies; project-scoped injection ignores user auto-approve; project-scoped availability hides user-only cookies | Failing regressions observed, then `bun test packages/core/src/credentials/inject-credential-tool.test.ts` passed twice before commit. |
| Discovered MCP tools drop abort signal | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/tool-system/mcp-manager.ts:518` | `c3163e35e31166573daecec85beac85ef45ac7f7` | `packages/core/src/tool-system/mcp-manager.test.ts`: `MCPManager discovered tool executors > forward the tool execution abort signal to client.callTool` | Failing regression observed, then `bun test packages/core/src/tool-system/mcp-manager.test.ts` passed twice before commit. |
| `ListMcpResources` filters out allowed resources | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/tool-system/builtin/mcp-tools.ts:83` | `b9cdd43bda99b01220ec093d2cbabf1a307a375f` | `packages/core/src/tool-system/mcp-manager.test.ts`: `MCPManager resource ownership > ListMcpResources keeps resources from allowed connected servers` | Failing regression observed, then `bun test packages/core/src/tool-system/mcp-manager.test.ts` passed twice before commit. |
| Local/zip/direct-git plugin installs skip var rewrite | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/plugins/installer/install.ts:45` | `eac53b98c49eab4820023fa9c97f748e39dfb28c` | `packages/core/src/plugins/installer/install.test.ts`: `installPluginFromPath > rewrites CLAUDE_PLUGIN_ROOT placeholders in the installed local copy` | Failing regression observed, then `bun test packages/core/src/plugins/installer/install.test.ts` passed twice before commit. |

Final focused verification: `bun test packages/core/src/credentials/inject-credential-tool.test.ts packages/core/src/tool-system/mcp-manager.test.ts packages/core/src/plugins/installer/install.test.ts` passed with `64 pass`, `0 fail`.

## Skipped / Deferred

- CDP findings: skipped by instruction because Batch 1 already fixed the CDP
  cluster and this batch was scoped outside `packages/cdp`.
- Explicitly out-of-scope architectural/runtime-evidence items: skipped by
  instruction, including engine split, arena extraction, steer step-boundary
  changes, compaction token-estimation rewrite, credentials partition mismatch,
  MCP login-shell PATH root fix, `NEEDS-DEEPER-VERIFICATION`, and
  `FALSE-POSITIVE / BY-DESIGN` entries.
- Red findings not selected for this localized batch: project settings
  `__proto__` smuggling, credential plaintext tool-output persistence, npm
  publish version gating, Electron predist lockfile materialization, and plugin
  executable trust gates. Each needs broader policy, recursive hardening,
  release workflow coverage, packaging workflow changes, or cross-surface
  sensitive-result design beyond this 3-5 fix batch.
- Failed MCP discovery cleanup: deferred after selecting the higher-confidence
  MCP abort/resource regressions; it needs a more direct connection-cleanup test
  around post-handshake discovery failure.
- Remaining desktop/TUI/core minor and major items: deferred because the batch
  target was reached and the selected fixes had clearer one-file regression
  coverage.
