# Fix Batch 5 - 2026-07-08

Scope: bounded, low-risk fixes from `docs/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. Batches 1-4 already handled the CDP cluster plus
several core, TUI, desktop, plugin, and infra items. This batch selected four
remaining focused findings with direct regressions and one local commit per
finding.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| Project settings `__proto__` smuggling | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/settings/manager.ts:595` | `46d2e82686b0bcc437091462fe1a0a4509fa0fa4` | `packages/core/src/settings/manager.test.ts`: `untrusted project cannot smuggle dangerous fields through __proto__` | Failing regression observed, then `bun test packages/core/src/settings/manager.test.ts` passed twice before commit. |
| Skill/agent markdown reads are root-unsafe | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/desktop/src/main/safe-read.ts:13` | `90fb6c0f45d3b336699aac9d8eb6eff57d5eac8f` | `packages/desktop/src/main/safe-read.test.ts`: unlisted markdown under arbitrary `.code-shell` is rejected; existing accept cases updated to register listed paths | Failing regression observed, then `bun test packages/desktop/src/main/safe-read.test.ts packages/desktop/src/main/skills-service.test.ts` passed twice before commit. |
| Delete key behaves like Backspace | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/tui/src/ui/components/TextInput.tsx:95` | `d523f298c0f72dae227041d7ec334eeb3abb6731` | `tests/text-input.test.ts`: `TextInput Delete removes the character at the cursor` | Failing regression observed, then `bun test tests/text-input.test.ts` passed twice before commit. |
| MCP image spills have no retained-file cap | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/tool-system/mcp-manager.ts:191` | `538008cf5533659c35af8fbb4078e2c695f028bc` | `packages/core/src/tool-system/mcp-manager.test.ts`: `deletes older spills after the per-server-tool retained-file cap` | Failing regression observed, then `bun test packages/core/src/tool-system/mcp-manager.test.ts` passed twice before commit. |

Final focused verification:

`bun test packages/core/src/settings/manager.test.ts packages/desktop/src/main/safe-read.test.ts packages/desktop/src/main/skills-service.test.ts tests/text-input.test.ts packages/core/src/tool-system/mcp-manager.test.ts`

Result: `92 pass`, `0 fail`.

## Skipped / Deferred

- Batch 5 stopped at four fixes, within the requested 3-5 range.
- Already-fixed findings from Batches 1-4 were skipped to avoid conflicts:
  CDP click/image/video refs, browser credential injection scope, MCP abort and
  resource ownership, local plugin var rewrite, async context cleanup, MCP
  discovery cleanup, `AddMarketplace`, `CODE_SHELL_HOME` layout fixes, config
  secret redaction, insecure plugin transports, `/image` size guard,
  `copy-assets.mjs`, and desktop replay timestamps.
- Explicitly out-of-scope items were skipped by instruction, including engine
  split, arena extraction, steer step-boundary changes, compaction
  token-estimation rewrite, credentials partition mismatch, MCP login-shell PATH
  root fix, `NEEDS-DEEPER-VERIFICATION`, and `FALSE-POSITIVE / BY-DESIGN`
  entries.
- Remaining red and major findings not selected here still need broader policy,
  release-flow, packaging, executable-trust, sensitive-result, or component
  workflow design: credential plaintext tool-output persistence, npm publish
  version gating, Electron predist lockfile materialization, plugin executable
  trust gates, release job permissions, CI lint/import guard hardening,
  browser-panel/session scoping, sensitive browser approval, provider key
  masking, async slash-command failure rendering, and notification routing.
- Other small candidates such as REPL max-context helper usage and prompt
  wording were deferred to a later batch because this batch already met scope
  with higher-severity, directly testable fixes.
