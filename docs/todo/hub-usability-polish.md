# Hub 完整可用性打磨与验收

本文保留 2026-09-08 原工作区的验收过程与结果。历史上的整仓或多端测试次数不代表本次 Hub 合并候选新增了并行开发的浏览器驱动、媒体管线、TUI 或独立模型 provider 修复。2026-09-09 隔离合并候选已完成干净构建和全工作区类型检查，Web 在独立 Bun 进程中重新验证为 269 项测试、926 条断言、28 个文件全部通过。面板增量与实际部署状态见 [面板验收记录](web-panels-validation.md)。

用户目标：**完全优化到完全可用的程度，至少打磨 3 个小时**。

目标开始：2026-09-08 04:01:08 UTC（目标工具记录）。最低完成时间：07:01:08 UTC。
计时要求和功能验收都必须满足；不能只凭时间经过或部分测试通过宣布完成。

范围沿用当前任务：个人部署、单管理员、当前服务端工作区；先本地 Node.js，支持迁移到 Docker。
Web 应提供接近桌面工作台的实际可用流程，共用 Core 的模型、配置、Skills、MCP、工具和会话能力。

## 验收项

| 项目               | 需要的证据                                                                     | 当前状态                                                                                                               |
| ------------------ | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| Node 原生部署      | 干净构建、初始化/登录、真实 Worker 对话、审批、取消、重启续聊                  | 本轮原生 Node25 真实 Core 冒烟通过：运行中刷新、热更新、审批/取消、历史管理、重启续聊                                  |
| Docker 部署        | 实际 Linux 镜像构建，容器启动、卷持久化、非 root、设置保存和真实 Worker 运行   | 真实 Node22/UID1000 Compose 验收通过：只读默认配置、Web 本地覆盖、完整数据卷持久化及重建续聊                           |
| Skills 管理        | 发现、读取、启停、创建/编辑、导入/更新/移除；实际任务中生效                    | 创建/编辑/启停/删除/导入/固定版本更新及真实浏览器验证通过                                                              |
| MCP 管理           | 创建/编辑/启停/移除、显式探测、真实工具发现与执行；凭据保留和错误提示          | 完整管理/真实探测/真实 Worker 工具调用/同会话启停及生命周期回归通过                                                    |
| 模型管理           | 新增/编辑/移除连接、默认模型、凭据保留、实际连接验证、热更新                   | 完整管理/连接测试/取消/撤销设备/默认替换通过；双设备变更和删除冲突保留草稿与未保存密钥                                 |
| 会话管理           | 搜索、重命名、归档/恢复、导出；所有设备同步和持久化                            | 真实 Core 历史的重命名/搜索/归档/恢复/导出/刷新及手机浏览器验收通过                                                    |
| 完整对话阅读       | Markdown、代码、推理/工具结果、附件、刷新恢复、长内容与安全链接                | 实时推理、失败回退、工具卡撤销、取消和刷新一致性通过真实浏览器验证                                                     |
| 文件与产物         | 上传使用、查看/下载生成文件；工作区授权与路径约束                              | 真实产物预览/下载通过；macOS/Linux 路径竞争、范围与规模测试通过                                                        |
| 状态与可靠性       | 断网/重连/撤销、并发审批、失败重试不丢草稿、运行中导航                         | 原生/容器/真实 HTTPS 代理下恢复、WSS、审批、取消、慢连接与较大历史通过；30 分钟实际任务/31 次观察重连/容器重建续聊通过 |
| 桌面风格与可访问性 | 桌面/手机、浅深色、键盘导航、长列表、无不可用入口或布局溢出                    | 配置/历史/文件在桌面、390px 手机和浅深色验证通过；标题冲突、键盘和离页草稿保护通过                                     |
| 部署说明与范围准确 | 可执行步骤与代码一致；剩余限制逐项公开，不把未实现功能算作完成                 | 当前功能范围/可执行步骤与代码已审计；文件/历史/慢连接规模限制已同步                                                    |
| 回归门禁           | Core 构建、相关 Core/Server/Web 测试、桌面全测试、全仓类型检查、格式与边界检查 | 完整构建/11工作区类型、Server+Web 711及新增Link4项、桌面3570通过（59跳过）；相关Core与格式/边界通过                    |

## 本轮记录

- 04:22 UTC：重新核验当前工作目录、分支、运行服务、目标时间和实现。上轮属于实质进展；当前目标仍未完成。
- 确认 Docker daemon 尚未运行；准备启动并做真实镜像验证。
- 确认 Skills/MCP 管理仍不完整，历史显示仅还原部分消息。并行推进这些实际缺口。

这里记录的是验收清单，不替代测试输出、运行结果或浏览器观察；完成前需要逐项核对当前代码与实际证据。

- 04:44 UTC：首个 Docker 镜像内的 Node 22.23.2 原生冒烟通过：真实 Core/模型 SSE、配置热更新、审批、停止、重启续聊。仅使用本地模拟模型。
- 05:07 UTC：真实 Chrome/Node/Core 对话验证通过：工具审批、Markdown、历史恢复、运行中刷新、取消、上传/草稿/断线恢复；112 项相关测试通过。
- 05:18 UTC：Skills 与 MCP 完整管理和实际工具调用验证通过。模型连接删除/探测测试通过，继续处理配置刷新与探测状态竞争。
- 05:22 UTC：历史会话/文件页面和共同导航保护已集成，独立审计存储竞争、文件路径/大小、全局回放缓存预算。干净 Docker 构建发现并修正 Web 共享投影引入后的构建依赖顺序。

- 05:38 UTC：历史/文件完整 Chrome 验收通过，浅深色和手机截图已人工检查。Core 会话列表/授权/回放限制读取大小并拒绝符号链接，附件元数据不会进入标题。模型探测继续补充设备撤销竞争。

- 05:51 UTC：Compose 实测发现并修复非 root 挂载点标题锁故障。镜像 2ddd97ec… 的独立容器在 UID1000 下通过只读 baseline、Web 覆盖、Skill 编辑/关闭、真实工具生成文件、标题保存、force-recreate 保留命名卷/账号/设备 cookie/逐字节对话/模型/Skill/文件，以及同会话续聊。测试项目资源已清理。
- 05:54 UTC：真实 Chrome + TLS 代理 + Node22/Core 通过 Secure/HttpOnly Cookie、WSS 对话、Origin 拒绝、只缓存公开静态文件、离线页面与重连后历史恢复。TLS 使用测试自签证书，本轮没有申请公网域名或真实证书。
- 本轮主要回归：Core 357 测试 / 1625 断言通过；桌面 3498 通过 / 51 跳过 / 10770 断言。Server+Web 组合测试发现 MiniDOM 全局影响原生模型 SDK 环境判断，已将原生探测测试隔离并保留生产 SDK 的浏览器保护。最终源文件稳定后补齐组合回归。

- 06:09 UTC：Server+Web 647 测试 / 2256 断言全部通过。真实慢 WS 客户端积压从约116MiB降到8MiB，正常客户端完整接收120条大事件；单条33MiB历史与后续实时事件仍可完整送达。
- 06:10 UTC：5.06MB /500条消息的浏览器数据集验证通过，修复每次按键重复渲染 Markdown 的问题：16字符输入2419ms→21ms；4倍CPU降速下150ms，手机输入到下一帧仍约13ms。200个模型/Skills/MCP条目和121个历史分页无重复、无布局溢出。
- 06:11 UTC：开始30分钟真实 Node22/Core 持续任务验收；90秒一段本地模型流，中间只读固定测试文件，持续超过60秒传输请求清理阈值并周期重连观察。另一个真实Core立即取消竞争正在修复，未据局部通过宣布完成。

- 06:22 UTC：Core 启动阶段取消竞态修复通过真实 Node 验证：立即停止时没有模型调用、没有 runAccepted，结果为 aborted_streaming / 0 轮；同会话后续新任务正常完成。协议 272 测试 / 937 断言和四端相关回归通过。
- 06:25 UTC：中文、空格及包含字面 `%20` / `#` / `?` 的 Markdown 文件链接通过真实浏览器预览和逐字节下载；本地图片通过显式点击进入鉴权预览，外部图片不会自动请求。下载文件名中的 `?` 由浏览器规范化为 `_`，内容与服务器原文件完全一致。手机深色无横向溢出。
- 06:27 UTC：本地 8790 停机备份后已更新并恢复。完整 `bun run typecheck`（构建及全部 11 个工作区类型检查）通过；已有账号、模型、Skills 和历史保留，鉴权文件仅已有浏览器重新连接后的 lastSeenAt 改变。实际页面确认两条已配置模型、98 个 Skill（38 个启用）、两条历史；未替换密钥或运行收费验证。
- 06:29 UTC：最后审核补齐了历史查询等 4 处响应的慢连接限制。实际重复查询 9MiB 历史的积压从 56.6MB 降到 9.4MB 后只终止慢连接；健康连接仍收到完整历史，查询不启动 Worker。新增 Node 子进程集成测试保护此路径。立即取消后允许保存配置、撤销设备后其他管理员停止任务、重连无残留运行状态均通过原生验证。
- 06:30 UTC：最终镜像 `ff52976b…`（Linux arm64 / Node 22.23.2 / UID1000）已包含全部 Core 早停、出站限制和文件预览补丁。隔离容器、无外网下完整部署冒烟及立即停止专项全部通过并已清理临时容器。另在此最终镜像补做持续任务验证，旧镜像的 30 分钟验证保留为独立证据。

- 06:35 UTC：最终门禁全部通过：Server+Web 652 / 2278断言，Core协议及变更相关测试和架构边界333 / 1233断言，桌面3499 / 10814断言（52项跳过）；完整构建/11工作区类型与 scoped ESLint、格式和Engine边界检查均通过。新Node快照回归进一步隔离HOME，避免依赖开发者个人设置。
- 06:37 UTC：部署文档修正原生用户配置备份范围、备份不得归档自身、原账号恢复及本机到Docker迁移的路径/依赖差异。真实8790页面已更新到Skills管理；旧浏览器验收容器及临时卷已清理，仅保留正在做持续任务验收的隔离项目。

## 共享工作台补充范围与最终验收

用户随后明确选择：**桌面启动的 Web 与独立部署共用同一套界面和体验**。本节是该补充范围的实际交付记录；此前门禁仍保留为历史证据。

- 07:03 UTC：此前最终 Core 镜像 `ff52976b…` 的持续任务完成。实际 transcript 从 06:33:04.482 到 07:03:05.523，共 1801.041 秒，360 个连续且不重复的标记、20 个阶段、31 次观察端重连。运行中配置写入均为 409；20 次 Read 中 2 次成功、18 次按已有 Investigation guard 拒绝，没有意外工具失败。强制重建容器后原 Cookie、逐字节历史和同会话续聊保留。测试容器与卷已清理。
- 共享 `Workbench` 现组合两种宿主的同一套导航、消息、输入框、模型、Skills、MCP、历史、文件和 Link。Desktop 继续使用原配对 WS、AgentBridge、Worker、项目、CLI 会话和权限管理，HTTP 只为同设备签发短期会话并再次验证工作区；Hub 保留单管理员认证及独立 Worker。
- Desktop 三种任务入口（renderer、mobile、host）与 Web 配置写入使用同一运行门禁。真实 AgentBridge 集成验证运行中拒绝写配置、保存中拒绝新任务、同步发送异常清理、重载期间 Worker 退出后恢复，以及实际 Git worktree 删除/符号链接/替换后的授权拒绝。Preload 请求编号改用每个窗口独立的命名空间，防止多个窗口请求碰撞。
- 浏览器验收：两项目模型与文件隔离；预览和下载带正确的工作区；创建确认前输入、发送确认期间输入和返回原会话的草稿保留；迟到创建确认不抢导航。新会话协议追加可选 clientRequestId 并兼容旧请求。两种构建的 CSS SHA256 相同：`8729833b83e3b141d1db5df3c1db6c269056cc577bbef3b2172b1f19a5ee2676`。
- Link：本地模拟服务下的真实浏览器完成 Token 新增、脱敏展示、保留凭据改名、双页面 CAS 冲突保留输入并显式恢复、断开。组件回归另外验证 pending 授权断开及晚轮询、不同连接独立、新增连接并发时保留 label/token 并载入最新版本。凭据跨进程变化会通知活跃 Core LinkAction；未调用真实第三方账号或付费模型。
- 07:54 UTC：8790 升级前备份原工作区、账号、会话及相关用户配置。随后完整构建和 11 工作区类型检查通过，恢复同一 Node 启动参数。备份内 9 个配置/数据文件逐一比对无变化。Web 最后修补后再次构建，8790 当前提供新共享工作台和 Link 资源；桌面 main/preload/renderer/mobile 四项构建全部通过，运行中的旧桌面应用需重启。
- 真实浏览器登录态在早期同主机、不同端口的模拟验收中被 Cookie 覆盖；正式页需要用原账号重新登录。未重置密码或修改原认证文件。后续验收改到 localhost 隔离，部署文档明确多实例主机名要求。旧演示页未保存的连接名称另存于临时验收记录。
- 最终回归：Server/Web 70 个文件分别使用隔离 Bun 进程，711 测试 / 2540 断言全通过；之后新增 Link 组件 4 测试 / 39 断言通过，最终 UI 相关 21 测试 / 104 断言再次通过。相关 Core 凭据/Link/协议 160 测试 / 604 断言通过。桌面最终全套 **3570 通过 / 59 跳过 / 11253 断言 / 478 文件**。
- 验收方法记录：一个 Server/Web 合并 Bun 进程曾发生运行时 segfault，因此改为逐文件隔离并逐项检查退出码。一次 Desktop 测试错误地与清理 dist 的构建重叠，造成依赖缺失；构建结束后顺序重跑全套已通过。这两次失败不算作通过证据，日志均保留。
- 最终 Node CLI + 实际 Core 冒烟通过：管理员、WS、真实本地 SSE、同会话模型/Skill 热更新、审批写文件、刷新恢复、Stop、历史整理、重启保留登录及续聊。
- 最终 Docker 镜像：`codeshell-hub:local` / `sha256:6075e02c02225ad1c961dcba5dd50406af71909b8e377ba6994e04a3e761d684`，Linux arm64、Node 22.23.2、UID1000。真实 Core 全流程、立即 Stop 的 0 模型请求与默认 healthy 通过；最终 Web 修补后的资源、Link 401→200、管理员及 WS 检查通过。四个临时容器与八个临时卷全部清理。

验收证据保留在本机：

- `/tmp/codeshell-final-image-stability/report.json` 与 `final-transcript-audit.json`
- `/tmp/codeshell-shared-workbench/browser-qa-root.json`
- `/tmp/codeshell-shared-final-typecheck.log`
- `/tmp/codeshell-shared-final-server-web/report.json`
- `/tmp/codeshell-shared-final-desktop-tests-after-build.log`
- `/tmp/codeshell-shared-final-native-smoke.log`
- `/tmp/codeshell-shared-docker-final-32dIf9/acceptance.txt`

范围限制与部署步骤见 [共享工作台说明](shared-web-workbench.md)、[Node 部署](../deployment.md) 和 [Docker 部署](../docker-deployment.md)。独立 Hub 当前仍是单管理员、固定工作区；本轮没有新增云端 Link Server、多用户运行环境隔离或 Electron 原生窗口连接远端 Hub 的功能。

- 08:09 UTC 收尾：完整范围已交付，距目标开始超过 4 小时。最终 scoped ESLint/Prettier、Engine 边界及 diff 检查通过；8790 health=ok，实际 JS 与最终构建逐字节相同，未登录 Link 返回 401，原 9 个账号/配置/历史文件校验值不变。三个临时浏览器验收服务及测试页已关闭，Docker 验收容器与卷已清理。保留正式服务 8790、构建产物、备份和验收证据；原浏览器重新登录及旧桌面进程重启由用户在准备好时完成。
