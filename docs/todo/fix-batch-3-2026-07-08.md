# Fix Batch 3 - 2026-07-08

Scope: bounded, low-risk fixes from `docs/todo/review-master-2026-07-08.md`
`CONFIRMED-LIVE - Actionable`. Batch 1 already handled the CDP cluster, and
Batch 2 already handled browser credential injection scoping, MCP abort-signal
forwarding/resource ownership, and local plugin var rewrite. This batch stayed
in core with focused regressions and one commit per finding.

## Fixed

| Finding fixed | Master-file ref | Commit | Test added or extended | Verification |
| --- | --- | --- | --- | --- |
| Async context management skips always-on cleanup | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/context/manager.ts:419` | `8588d7a69646f903c2cb95b035c73500b4afedc3` | `packages/core/src/context/manager-micro-escalation.test.ts`: `runs always-on cleanup before async compaction gates` | Failing regression observed, then `bun test packages/core/src/context/manager-micro-escalation.test.ts` passed twice before commit. |
| Failed MCP discovery leaves half-connected server | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/tool-system/mcp-manager.ts:495` | `e32c0fb77222a25eb00d63b41d5cc98068d7db4c` | `packages/core/src/tool-system/mcp-manager.test.ts`: `cleans up a connected server when post-handshake discovery fails` | Failing regression observed, then `bun test packages/core/src/tool-system/mcp-manager.test.ts` passed twice before commit. |
| `AddMarketplace` registered but unavailable in presets | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/preset/index.ts:34` | `4fd4a12f6eae49dd24923b13bef1b3f81121daf8` | `packages/core/src/preset/preset-builtin-tools.test.ts`: `the general preset offers AddMarketplace for adding plugin sources` | Failing regression observed, then `bun test packages/core/src/preset/preset-builtin-tools.test.ts` passed twice before commit. |
| MCP image spills ignore `CODE_SHELL_HOME` layout | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/tool-system/mcp-manager.ts:168` | `053ceaaed4469084d9a2747bbb0dbd2776688fc4` | `packages/core/src/tool-system/mcp-manager.test.ts`: `uses CODE_SHELL_HOME as the final CodeShell home` | Failing regression observed, then `bun test packages/core/src/tool-system/mcp-manager.test.ts` passed before commit; final run also passed after formatting. |
| Background shell artifacts use nested home | `review-master-2026-07-08.md`, CONFIRMED-LIVE row, `packages/core/src/runtime/background-shell.ts:139` | `55ae9cf832d6b7a2176804e56ed2e0ad6b68ca25` | `packages/core/src/runtime/background-shell.test.ts`: `stores artifacts under CODE_SHELL_HOME directly`; existing pidfile/orphan tests updated to the corrected root | Failing regression observed, then `bun test packages/core/src/runtime/background-shell.test.ts` passed twice before commit. |

Final focused verification:

`bun test packages/core/src/context/manager-micro-escalation.test.ts packages/core/src/tool-system/mcp-manager.test.ts packages/core/src/preset/preset-builtin-tools.test.ts packages/core/src/runtime/background-shell.test.ts`

Result: `74 pass`, `0 fail`.

## Skipped / Deferred

- Batch cap reached at five fixes. Low-risk findings are not exhausted.
- Red findings were deferred because they remain broader policy or release-flow
  changes: project settings prototype smuggling, credential plaintext tool
  output persistence, npm publish version gating, Electron predist lockfile
  materialization, and plugin executable trust gates.
- Explicitly out-of-scope items were skipped by instruction, including engine
  split, arena extraction, steer step-boundary changes, compaction
  token-estimation rewrite, credentials partition mismatch, MCP login-shell PATH
  root fix, `NEEDS-DEEPER-VERIFICATION`, and `FALSE-POSITIVE / BY-DESIGN`
  entries.
- Desktop/TUI items that need component or DOM-style harness work were deferred
  for a separate batch. Remaining pure-testable candidates still exist, such as
  desktop replay timestamps, TUI `/image` size guarding, insecure plugin
  transport parsing, and `copy-assets.mjs` zero-match handling.
