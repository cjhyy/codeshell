# 本地／云端项目、跨设备 Panel、Link：总实施清单

更新：2026-09-26。状态：实施中，尚未发布完整版本。

最新优先级：先完成电脑本地＋云端业务闭环、真实 Link 接入、三仓兼容发布与部署恢复；手机 Panel 的触控／窄屏操作优化后置。手机远程连接与界面优化分别验收，不以界面调整替代设备连接能力。

## 产品原则

同一个 Panel 产品面向本地项目和云端项目。项目决定执行环境、文件位置、数据、后台任务及安装版本；电脑和手机是操作入口。打开同一个项目必须看到同一套项目状态，切换设备不迁移任务。不同项目允许选择不同 Panel 版本，能力差异由 Host 显式声明。

- 电脑本地项目：电脑执行；云端服务离线不妨碍本地操作。
- 电脑云端项目：独立云端工作台登录，云端执行。
- 手机本地项目：配对并远程控制电脑；电脑离线时显示离线。
- 手机云端项目：直接登录云端；不依赖电脑在线。
- Link：独立管理第三方连接和受限授权；其故障只影响依赖该连接的功能。

## 仓库与部署边界

| 仓库 | 负责 | 不搬入的内容 |
| --- | --- | --- |
| `codeshell` | Core、Desktop、共享 Web、Panel Host、项目执行和连接基础包 | Panel 的模型、编辑规则、业务脚本 |
| `codeshell-panel-apps` | 六个 Panel、业务原生入口、客户端辅助库、独立版本 | Agent 引擎、服务器身份和主机权限 |
| `codeshell-services` | 独立 Link、云端控制产品入口、部署配置、运维工具 | Core 源码、桌面仍依赖的整个 `packages/server` |

服务仓库已建立私有远程仓库 `cjhyy/codeshell-services`，main 已推送；公开发布尚未完成。它通过固定公开包版本组合云端产品。拆仓库与拆部署单元分别安排；首版仍可在一台服务器部署。

服务器至少有三个独立运行单元：

1. **Cloud 控制服务**：账号、初始化、项目目录和生命周期、工作台入口、项目路由。
2. **项目执行环境**：各项目 Docker 容器和数据卷，实际运行 Agent、Panel 任务及程序。
3. **Link 服务**：第三方授权、下游应用授权、凭据加密和只读数据动作。

反向代理负责 Cloud / Link 各自域名与 HTTPS。电脑执行器属于桌面产品；后续安全中继是另一项连接能力，不能用 Link OAuth 服务替代。现阶段 Cloud 控制服务是宿主机 Node 进程，不直接套用单工作区 Compose。

## 当前实际状态

“已验证”仅表示下列测试环境通过，不能替代正式上线或真实手机验收。

| 工作项 | 状态 | 证据及限制 |
| --- | --- | --- |
| 执行环境稳定身份、认证后发现 | 已验证 | Desktop、Hub、项目控制服务；损坏记录和并发初始化检查 |
| Web 保存电脑／云端入口 | 已验证 | 390/1440px Chromium；按站点保存，非统一账号设备目录 |
| 原生桌面打开云端工作台 | 已验证 | 生产 Electron；独立登录、创建云端项目、重开、会话隔离 |
| 两个云端项目隔离与重启持久化 | 已验证 | 真实 Docker、文件和会话、主 Agent 与 Panel 任务；模型为测试服务 |
| 独立 Link 上下游 OAuth、管理、撤销 | 测试闭环通过 | 真实 HTTP/浏览器/容器，GitHub 上游为受控响应；真实账号未验收 |
| Link 加密备份与离线恢复 | 已验证 | Node + Docker，恢复会撤销旧下游授权，避免恢复已撤销权限 |
| Panel 多设备条件保存 | 公共接口已验证，下载 Panel 和投资 Panel 数据源／价格技术提醒记录已接入 | Electron/Web 跨进程竞争、HTTP、冲突 UI；投资配置有双浏览器与实际 Node Host 验证，其余编辑数据仍待接入 |
| 自动化创建去重与配对 Web | 共享调度存储、原生桌面与配对 Web 已接通，投资提醒已接入 | 同一 Panel／工作区／绑定任务／业务 key 的保留记录原子去重；四进程及实际 Electron＋配对 HTTP 共用任务 ID、查询／修改／暂停／删除验证通过。云端已组合项目共享 Worker、审批与持久执行回执，真实 HTTP／WebSocket／Core Worker 使用受控模型完成文件写入、跨登录及重启恢复；删除后重建及旧无 key 记录不在长期去重保证内 |
| 下载原生后台入口与界面接入 | 桌面主项目匿名／测试账号下载已验证 | 真实 Electron 两任务队列、关闭／重开、HTTPS Cookie、MP4 字节；真实服务商、云端和手机业务验收仍待完成 |
| 桌面与配对 Web 共用原生后台任务 | 已验证 | 真实 Electron＋配对 HTTP；同一任务 ID、双向查询／取消、退出与关闭远程服务不中断；旧 Web 记录只读保留 |
| 桌面／配对 Web 目录书签 | 主项目流程已验证 | 同一目录重复选择保留授权；旧 Web 标识迁入共享记录，替换目录失效；worktree 作用域仍待统一 |
| 后台任务向授权目录交付产物 | 匿名下载界面已接入 | 真实下载、Electron／配对 HTTP、Linux 文件交付测试；同内容复用，不覆盖不同内容 |
| 后台队列暂停／并发设置 | 下载界面已接入 | 持久设置、跨设备版本冲突、整队 128 项、Electron／配对 HTTP 共同控制 |
| 后台 Cookie 凭据交付 | Host 与下载 Panel 已接通，完整业务验收进行中 | 私钥持久化、独占管理、崩溃遗留文件清理、桌面／配对 Web 同版本账号、Hub 项目账号、提交及重试确认已验证；下载脚本、账号版本保存、显式换账号重试及窄屏界面已接入，真实 Electron HTTPS 账号下载及文件清理通过，尚非真实服务商与手机验收 |
| 带账号的视频信息读取 | 桌面真实流程与配对 HTTP 已验证 | 版本化临时进程授权、撤销与清理；下载队列暂停时仍可读取；使用测试账号，真实站点／物理手机尚未验收 |
| 窄屏网页完整账号下载路径 | 390px Chromium＋真实 Desktop 通过 | 正式局域网配对、桌面任务恢复、账号信息读取、新增后台下载、关页／重开、浏览器保存精确文件字节；不是物理手机或真实服务商验收 |
| 下载历史的浏览器预览与保存 | 390px 配对／云端浏览器已验证 | 真实 MP4 解码与下载字节一致；云端退出发起登录、第二登录接续及项目重启保留同一任务／资源通过；尚非物理手机验收 |
| Hub 原生任务归属项目 | 已验证 | 已接收任务不因发起登录退出而取消，同项目其他登录收到进度；准备／审批仍随登录撤销，项目停止中断协调器 |
| 云端下载跨设备完整匿名流程 | 真实 Docker＋浏览器已验证 | 实际安装包、yt-dlp、两次独立登录、390px 预览下载及重启恢复；媒体为生成夹具，真实服务商及云端账号流程未验收 |
| 六个 Panel 的四种组合完整流程 | 未完成 | 不能将页面能打开记为业务验收通过 |
| 项目锁定 Panel 包版本 | Core／Skill、Hub 和 Desktop／配对 Web 的包选择与执行已接入 | 真实 Electron 在全局 2.0／项目 1.0 下共享任务、账号和队列通过；双项目版本／页面隔离以 Electron mock＋真实 HTTP／Node 验证；桌面条件绑定、项目安装／升级审阅和共享 Panel 执行占用检查已接入；旧绑定已在项目发现时批量固化，未打开项目保留迁移基准；任务记录包身份、正常版本恢复及缺失／损坏包的保留版本修复已接入；配置修复、数据迁移和完整回滚未完成 |
| Host 调用独立 Link | 执行链、共享授权管理、Hub／配对 Web、Electron 云端窗口、原生 Link 管理和真实 Docker 项目已接入 | 上游受控；两个真实项目容器通过 HTTPS、390px 浏览器授权、原项目回调、错误项目拒绝、容器内 LinkAction、重启持久化及断开撤销；原生多账号、改名／重连／取消、启动清理通过；真实账号、物理手机和生产部署验收待完成 |
| 公网部署及云端恢复／回滚 | 未完成 | 缺真实部署配置与演练，未发布新版本 |

设计 Panel 最新进展：项目隔离 PR #32、完整备份 PR #33、旧日志导入／另存恢复 PR #35、事务回滚保护 PR #36 和保存期间编辑保护 PR #37 已通过最终 CI 并合入；页面／资源加载归属 PR #38 与设计交付归属 PR #39（含导出源版本回执）也已通过最终 CI 并合入。完整备份包括页面及图片／字体；旧日志必须有可验证的基础文档和资源。这些进展不代表六 Panel 完整业务或跨版本升级已验收。

详细命令、测试数量与未解决异常见 [交付记录](project-cloud-panels-delivery.md)。完整原始范围见 [目标原文](project-cloud-panels-objective.md)。

## 工作包 A：项目身份与操作路由（codeshell）

- [x] 建立稳定执行环境 ID，提供受认证的发现接口。
- [ ] 将 `environmentId + projectId` 贯穿项目列表、打开、上传、保存、审批、取消与下载。
- [ ] 桌面注册表和云端注册表用适配器表达统一项目信息，保留原有数据布局。
- [ ] 统一项目名称、执行位置、在线状态、Host 能力提示。
- [x] 共享 Web 工作台顶栏持续显示当前项目／工作区与执行位置；390px 实际双 Docker 项目切换显示正确，不依赖展开侧栏。
- [ ] 所有在途请求固定发起时的目标项目；切换项目后不能把旧结果写入新项目。
- [x] 投资 Panel 的 Host 请求队列按项目代次隔离：旧排队请求不发出，旧响应不交给新项目，程序／目录发现缓存不跨项目复用；不宣称撤回已执行动作。
- [x] 投资资讯与市场脉搏控制器固定整条操作的项目代次：文件读写、任务查询／修改、通知批次、手动同步和外链确认在切换后停止后续调用；旧结果及收尾不能覆盖新项目。覆盖各请求边界成功／失败的延迟返回及控制器重置；完整投资浏览器回归通过，其他控制器仍需逐项验收。
- [ ] 手机仅提交受授权项目身份，由 Host 解析实际目录。
- [ ] 验证断线不切换执行地点、不向另一个环境转发凭据。

完成标准：连续切换两个环境和两个项目时，文件、任务、授权、结果都落在原定项目；同名项目也不会串线。

## 工作包 B：共享 Host 与数据／后台任务（codeshell + Panels）

- [ ] 逐步共享桌面和服务端的授权、资源、进程、任务、存储服务；保留旧调用兼容。
- [ ] 统一能力探测、结构化错误、事件、大小限制和确认行为。
- [x] 提供条件保存接口，沿用旧存储格式与共同文件锁。
- [x] Desktop 持有唯一原生任务协调器，配对 Web 复用其项目任务、执行版本和事件；旧 Web 历史保留。
- [x] Hub 已接收的原生任务归属项目，发起登录退出后继续运行；其他授权登录可接收进度并读取结果。
- [x] 下载页面发现另一设备新增的已提交记录，不覆盖本地草稿或推进条件保存版本。
- [x] Hub 目录书签与文件锁位于可写数据卷，兼容只读容器根目录并迁移旧书签 ID。
- [ ] 将需要跨设备继续编辑的草稿、工程、队列、历史全部保存在 Host。
- [ ] 其他 Panel 接入版本校验与明确的冲突处理；滚动位置等界面状态可留设备。
- [x] 投资 Panel 数据源配置采用项目条件保存，冲突保留草稿、可下载备份并明确读取最新配置；受控双浏览器和实际 Node Host 存储验证通过。旧 Host 显示能力限制，其余投资编辑数据尚待迁移。
- [x] 投资价格／技术提醒的关注记录、检查结果和事件使用条件保存；冲突保留页面修改，提供备份／明确重读，保存成功才提示完成。提醒操作前检查保存版本，异步步骤绑定项目代次。
- [x] 共享调度存储支持原子唯一创建，原生桌面按 Panel／工作区／绑定任务派生身份；投资提醒发现新方法后使用固定市场 key。响应丢失先查询结果，不降级重发；旧 Host 显示并发创建限制。
- [x] 投资资讯同步（A 股／美股）与市场脉搏接入相同唯一创建能力和独立固定 key；三类任务创建结果或核对结果不明后的首次重试只读取，即使另一设备修改定义也不自动覆盖。真实独立 CronStore／调度器联测通过；仍限定同一绑定任务，不自动合并旧记录。
- [x] 配对 Web 接入桌面唯一调度器，支持查询、创建、唯一创建、更新、暂停、恢复、立即运行和删除；校验持久会话与目标工作区，异步读取后复验授权，持久变更在文件锁内检查最新任务归属。实际 Electron／配对 HTTP 双向管理通过，执行回调用受控测试，尚非真实模型提醒验收。
- [x] 提供云端项目持久调度构件：独立数据文件与单实例租约、保留 Panel 修订身份、严格读取、持久 Session 校验、准备期间变更检查、权限撤销停用、升级占用及等待执行退出后释放租约。真实定时器和独立 Node 进程锁竞争通过；执行器／包授权检查由组合层注入，生产 Hub 已接入。
- [x] Worker 协议补齐按轮次的沙箱模式与后台 Shell 限制，队列／后续唤醒保留本轮策略，下一普通轮次不继承临时限制；网页不得提交这些 Host 专有参数。真实 Node Core 在已有 Session 写入文件、拒绝后台 Shell、同 Worker 继续普通轮次通过；不等于调度执行组合已接通。
- [x] 云端项目接通长期调度器、Panel 自动化接口和共享 Worker 执行生命周期：包权限校验、独占持久 Session、无人值守审批、关闭等待、持久运行回执、失联暂停及重启核查。真实 HTTP／WebSocket／Core Worker 使用受控模型写入实际文件，跨登录接续及重启保留结果通过。
- [ ] 云端自动化补真实模型、手机业务界面、提醒产物和通知验收；不能以共享 Host 接口及受控模型通过代替六 Panel 四组合验收。
- [x] 投资价格／技术提醒、资讯同步和市场脉搏的新任务通过项目选定包的 Skill 定位程序；手动资讯／市场脉搏共用相同路径。实际安装两版本 Quant 包、双项目目录与 Node 引擎执行验证通过；旧持久任务不自动重写，真实模型执行另验。
- [x] 提供自动化定义版本与条件更新／删除：Desktop、配对 Web 和 Hub 在存储锁内检查，同一任务的运行统计不制造编辑冲突。投资行情关注、资讯和市场脉搏已接入，冲突后只读取、不自动覆盖；真实 Electron／HTTP 跨端竞争通过。
- [x] 投资长期个股／板块关注接入条件保存：冲突保留草稿、下载与明确重读；严格读取阻止丢项，保留兼容扩展字段，项目切换停止旧操作，暂停扫描不影响保存。203 项离线测试、390px 完整浏览器回归及实际 Node Host 磁盘联测通过；真实手机／服务商另验。
- [x] 投资回测参数接入条件保存、冲突／失败提示、原始输入备份与当前项目载入；项目切换阻止旧队列写入和迟到策略读取。完整界面与本地检查通过，PR #27 待远程验收和合入，未发布；无效草稿仅保留于当前页面。
- [x] 设计 Panel 自动恢复草稿接入读取版本、条件保存和条件删除；冲突／读取失败／不明写入阻止自动覆盖，损坏记录保留。切换项目停止旧请求，当前画布与旧项目未确认草稿可下载备份，明确读取最新恢复记录。11 项单元及双窗口实际浏览器检查通过，完整离线检查与包校验通过；已维护真实 Node Host 磁盘联测脚本并通过竞争、重启、隔离和撤销检查。PR #29 待最终组合验收与合入。页面内未确认草稿不是持久备份，备份导入、完整迁移与真实云端业务仍另验。
- [ ] 其余存储迁移、历史任务升级、其他 Panel 条件操作迁移与完整提醒产物／通知验收。
- [x] 主项目的 Desktop／配对 Web 共享目录书签，保留旧标识并检查目录身份。
- [x] 后台任务通过作用域书签取得目的目录，下载程序完成逐文件校验与非覆盖交付。
- [ ] 统一 worktree 与主项目的目录书签作用域。
- [x] 公共原生队列提供持久暂停／并发设置、跨设备条件更新和整队容量。
- [ ] 下载等长任务从页面队列移入后台，取得稳定任务 ID。
- [x] 后台账号的 Host 私钥持久化、崩溃遗留文件清理、明确确认和桌面／Web 适配。
- [x] 下载原生入口和 UI 接入后台账号选择及授权，普通重试保留原账号版本，显式换号创建新任务。
- [x] 带版本校验的临时账号授权；下载信息读取接入，桌面真实 HTTPS 与配对 HTTP 程序读取通过。
- [ ] 完成下载真实业务四组合验证，包括登录采集、带账号的信息读取和真实服务商。
- [ ] 请求去重、先读快照再接事件；请求结果不明先查询，不盲目重跑。
- [ ] 关闭页面、断网、撤销、取消、停止项目、Host 重启的任务行为逐项定义和验证。
- [ ] 输入资源交付完成才启动任务；输出进入项目资源库，供预览、导出、下载。
- [ ] Agent 和页面复用 Panel 的后台业务入口，无人值守任务不依赖页面注册工具。

完成标准：关闭页面后后台任务仍按约定运行；另一设备能查询同一任务和输出，重复提交不会重复执行有副作用的操作。匿名和已保存账号下载已按任务逐项提交后台，未确认提交的项目仍须显式恢复；真实服务商与全部环境业务验收未完成，不能据此声称全部长任务已完成。

## 工作包 C：Panel 版本与升级（codeshell + Panels）

- [x] 安装器保留按内容摘要寻址的历史包；重复内容复用，解析时校验完整包，缺失／损坏不跟随最新版。
- [x] 保留旧可变安装目录兼容，升级前保留有效旧包；最终提交检查拒绝后清理暂存副本。
- [x] Core 提供项目 pin schema、严格读取和包选择；Skill 按项目 pin 校验并读取同一版本，worktree 继承主项目。
- [x] Hub 新绑定／已审阅安装条件保存版本和包摘要；更新同时校验项目状态与全局安装状态，安装后冲突不覆盖其他设备绑定。
- [x] Hub 页面和原生任务读取项目包；真实 HTTP 两项目执行 1.0.0／2.0.0，旧页面授权与重启后的旧任务结果保持。
- [x] Desktop descriptor／protocol／inspection cache 按项目选择包；页签 ID 保持稳定，页面与 Agent 工具按项目变体选择。
- [x] 配对 Web 启用同一项目选择并共用原生任务；手机绑定变更通知桌面，旧 guest 在 pin 改变后拒绝执行。
- [x] 真实 Electron＋配对 HTTP 验证项目 1.0 保持运行而全局为 2.0，账号、队列、任务 ID 和结果一致。
- [x] 桌面两个绑定入口复用 Host 条件写入，保存项目版本及摘要；拒绝陈旧修改，普通设置接口不能绕过。
- [x] 桌面项目安装／升级使用窗口与项目限定的审阅凭据，读取项目原始来源；确认后通过共享 Host 安装并条件更新 pin。
- [x] 旧项目发现／管理时批量固化现有绑定，保存准确包引用；全局更新／卸载前保留迁移基准，未打开项目不自动跟随新包。
- [x] 同步 Skill 读取遵循同一迁移基准，迁移不覆盖另一设备的明确 pin／解绑，不新增绑定或改变开关。
- [x] 缺失／损坏包与旧迁移基准故障按 Panel 隔离；桌面／Web 提供保留版本审阅修复，重新确认全部权限，保留项目数据和任务记录。
- [ ] 损坏项目配置、无可用保留包、无法写入配置等异常修复，以及部署回滚验收。
- [x] 实现管理员离线项目配置检查、完整 JSON 审阅修复与原始字节回滚：读取不写文件，双摘要防陈旧修改，使用共享设置锁、私有持久备份和原子替换，保留 YAML 备选文件；真实 Node CLI 与设置回归通过。PR #16 全部远程 CI 通过并合入 main `520d8b2a`，未发布；不是在线修复 UI，也不替代包／文档或服务器回滚。
- [x] 三仓候选中的六个真实包在双 Docker 项目实际安装、HTTP 绑定、内容摘要锁定、受控更新拒绝／审阅切换／回退，五个有存储权限的包保持独立数据；移除来源后停止／重启仍选择原包。Linux CI 36242780285 通过，Host PR #19 已合入；HTML 标记更新不代替业务数据迁移或完整 UI 验收。
- [x] 原生桌面双项目窗口通过真实安装、增加权限更新、历史回退和损坏包修复界面验收；确认前不提交、修复不改变另一项目 pin、保留已有文档字节。修复新窗口项目快照晚于渲染造成的空白窗口；PR #20 合入 `d719d7af`，完整 CI 36244792129 通过。使用专用测试包，不替代六个 Panel 的业务迁移。
- [x] 完整 Web 工作台连接两个真实 Node Hub，点击审阅安装、绑定、增加权限更新、历史回退和损坏修复；真实 iframe 分别保存文档，来源失败保持旧包，删除来源后退出整个 Host 进程再启动仍能读回版本和各自文档。仅 GitHub 上游 commit／ZIP 受控，浏览器 HTTP／Core 安装／授权和存储真实；PR #21 合入 `1841d7df`，最终完整 CI 36246501954 通过。
- [ ] 将完整安装与恢复界面继续覆盖 Docker 云端代理入口和六个实际 Panel 业务；单机 Node Hub 与合成文档不能替代目标部署或领域数据迁移。
- [ ] 安装包不可变；项目升级不覆盖其他项目正在使用的包。
- [x] 共享 Panel 执行入口与包修改使用同一进程内准入检查，涵盖准备、队列、临时进程、Agent 子任务和页面工具；等待实际退出／清理后才允许修改。
- [x] 项目升级不阻断另一个已固定版本项目的执行；解绑只检查目标项目，全局卸载检查所有项目。
- [ ] 完整版本升级／恢复验收，包括旧任务历史、持久中断记录、数据迁移与回滚；当前准入检查不代替跨进程执行协调。
- [x] 新原生后台任务记录 Host 选定包版本与摘要，准备完成、执行和重试前重新校验；重启保留原记录，旧无版本历史只读。完整升级／恢复界面仍单独验收。
- [x] 正常项目可在桌面或 Web 查看保留版本、审阅权限并恢复项目 pin；恢复前校验包内容、审阅归属与版本，运行任务阻止切换；不改变其他项目和文档数据。
- [x] Video Studio 建立升级前原文档的持久目录，超出普通 20 次历史仍可恢复／导出原格式；迁移前完成目录条件保存，恢复先归档当前修改，失败／过期操作不替换工程。实际 Host 存储、IndexedDB 与完整应用浏览器验证通过；这是文档恢复，不等同自动降级安装包或媒体／服务器整体回滚。
- [x] 求职大快照改为独立、只创建的分片 v2，最后按根文件版本提交；两个窗口竞争和旧读者不再被复用的 A/B 文件覆盖。旧 A/B 可读，替换前保存原根备份，备份失败不提交；37 项及完整 CI 通过，Panel PR #40 合入 `ed7d62bd`。旧包不能读取新格式；完整恢复入口、历史分片保留策略和其他数据迁移仍待完成。
- [x] 求职根 v1 升级不再被语义无变化优化跳过；根版本／存储迁移前独立保存原根文本和全部引用分片，分块上限、摘要／长度／结构校验、完成标记和项目作用域保护齐备。删除原文件后仍可只读重建，失败后明确重试完成升级；47 项、本地完整检查和两轮完整 CI 通过，Panel PR #41 合入 `6a762e2b`。恢复选择／预览／确认、损坏当前根的原文保护、草稿防回放和保留策略仍待完成。
- [x] 求职快照选择／预览／确认恢复、损坏原文保护、恢复前内容归档及旧草稿防回放已实现，本地 68 项／完整检查／包预检通过；Panel PR #42 的 CI 36252407120／36252384368 全部通过并合入 `b1ce6c08`。外部资源、后台任务和历史清理不在本项内；真实云端代理验收继续单独补充。
- [ ] 新权限重新确认，文档迁移可检测、可备份、可恢复。
- [ ] 升级失败保留旧数据和可用旧版本，记录兼容矩阵。

完成标准：两个项目使用不同版本互不影响；手机与电脑进入同一项目时版本一致；更新不能让旧任务突然执行新脚本。

## 工作包 D：六个 Panel 完整适配（codeshell-panel-apps）

每项都覆盖电脑本地、电脑云端、手机本地、手机云端，并使用真实产物验收。

| Panel | 必须完成的业务流程 | 特别检查 |
| --- | --- | --- |
| 下载 | 搜索／添加 → 队列 → 后台下载 → 取消／恢复 → 预览／保存 | 队列脱离页面、目录授权、跨平台依赖、账号选择 |
| 求职 | 资料／职位 → 文档编辑 → 录音／转写 → 自动化 → PDF 下载 | 手机录音、文档冲突、个人数据所属项目 |
| 投资 | 数据源 → 更新 → 研究 → 保存 → 自动化／提醒 | 连接范围、定时任务、来源与时效 |
| 视频 | 导入／采集 → 编辑 → 处理 → 保存 → 导出／下载 | 大文件资源、后台渲染、设备录音、恢复 |
| 设计 | 创建／导入 → 测量 → 画布编辑 → 预览 → 保存／导出 | 触控、窄屏、坐标、资源引用、编辑冲突 |
| Starter | 安装 → 绑定 → 基础调用 → 切换项目 → 重开 | 共享契约示例和兼容旧 Host |

所有模型、依赖版本、安装策略和业务规则留在 Panel。无法在 Linux/手机完成的操作需要经过验证的适配，不能仅隐藏按钮。

实际局域网浏览器验收发现：普通 HTTP 页面不提供 `crypto.randomUUID()`。
下载及 Video Studio 已改为安全随机数兼容实现并通过回归；视频的工程新建、保存和重开
已在关闭 `randomUUID` 的浏览器中验证，不代表全部手机流程完成。
下载历史在声明 `resources.open` 的 Host 上已改为浏览器预览和设备下载，
定位操作打开认证目录列表；未声明该方法的桌面保持原播放器流程。真实配对
390px 配对和真实 Docker 云端浏览器已解码实际下载的 MP4 并保存相同字节；
云端还验证独立登录间接续和项目重启恢复。真实服务商、云端带账号路径和物理手机仍待验收。

## 工作包 E：设备入口和远程连接（codeshell，后续 services 产品层）

- [x] Web 环境入口保存与切换；不保存配对／初始化令牌到地址书。
- [x] 原生桌面云端窗口，隔离云端页面和本地系统权限。
- [ ] 电脑配对、云端登录、项目／Panel／审批／结果整合到手机工作台。
- [ ] 手机上传、项目选素材、预览、下载；持续显示执行位置。
- [ ] 窄屏、触控、软键盘、横竖屏、长列表验收。
- [ ] 物理手机锁屏、后台切换、弱网与重开恢复。
- [ ] 稳定设备身份和远程地址，主机出站连接、在线状态、断线重连、设备撤销。
- [ ] 统一设备／项目目录、安全中继及任务完成／失败／等待审批通知。
- [x] 云端窗口独立 Link 同域登录／授权跳转及回调，保留窗口权限边界；实际 Electron 测试通过。
- [ ] 云端窗口真实录音／通知设备验收及真实服务商授权。

完成标准：手机能明确选择电脑或云端项目，看到原任务；主机离线状态可信，撤销设备后旧连接不能继续操作。

## 工作包 F：独立 Link（services + codeshell）

- [x] 单 owner 的独立入口、管理页面、配置、持久化与加密存储。
- [x] 下游应用登记、明确同意、授权码 + S256、访问／刷新令牌、范围控制和撤销。
- [x] GitHub 上游适配与只读动作；刷新、过期、撤销、在途结果和恢复路径的受控测试。
- [ ] 真实 GitHub App 完成授权、账号识别、只读动作、撤销闭环。
- [x] Core 提供远程 Link 授权交换、受限动作和令牌轮换，桌面 worker 通过 Host IPC 执行；真实独立服务验证协议，Host 保留凭据。
- [x] 共享 Host 管理／HTTP 支持受信配置、owner 绑定的授权发起／回调、条件保存、取消、过期、快照和断开撤销；远端撤销失败禁用本地记录并允许重试。
- [x] Hub CLI／SDK 配置、浏览器授权跳转、回调、改名／重连／断开入口已接入；真实 Node Hub＋独立 Link 在 390／1440px 浏览器完成同意、拒绝和撤销，上游为测试账号。
- [x] 配对 Web 移动端回调／原工作区返回与 Electron 云端窗口授权；实际程序验证错误项目和撤销设备不能完成回调。
- [x] 原生桌面 Link 管理入口；隔离授权窗口、多个明确账号、改名、重连、取消与断开，真实 Electron＋独立 Link 测试通过，上游受控。
- [x] 真实 Docker 项目授权：两个隔离容器、受信测试证书的 HTTPS、390px 授权与原项目回调、错误项目拒绝、容器内只读操作、重启后同一连接和远端撤销。
- [ ] 稳定 HTTPS 隧道、物理手机、真实账号及真实公网域名验收。
- [x] 已取得令牌的未保存授权和被替换旧 grant 的持久清理：条件写入时保留、私有加密边界、跨进程租约、退避和重启恢复；真实 Electron 无需打开 Link 页即可撤销遗留授权，真实独立 Link 验证通过。令牌未收到／未落盘及磁盘故障不属于可保证恢复的状态。
- [ ] 明确选择连接；持有受限 Link 授权，不暴露 GitHub 原始 token。
- [ ] 对齐现有本地动作与远程动作的参数／结果形状，保持本地连接可用。
- [ ] 断线、刷新结果不明、撤销、重新授权展示清楚；Link 离线不拖累其他功能。

首期是单实例 Link；横向扩容涉及跨进程刷新协调，另行设计。

## 工作包 G：部署、运维、仓库与发布（services + codeshell）

- [x] 本地服务仓库、固定公开包依赖，无跨仓库源码路径引用。
- [x] Cloud 产品入口、Link 镜像／Compose、systemd、HTTPS 配置模板。
- [x] 建立私有远程服务仓库和 main 基线，配置 Node 22.16／22／24 的测试、格式和浏览器 CI，并提供手动触发入口。
- [x] services 真实 GitHub Actions CI：Node 22.16／22／24 的安装、测试、格式与浏览器验证全部通过，运行 35964638616。
- [x] 实际 tarball 独立安装、版本／能力／资产发布检查、同包集项目镜像及双项目 Docker 验证；最新 Linux 候选 CI 35969080635 全部通过（Host c0af86ba、services 04b058c），后续修改另行验收。
- [x] 保存可搬移的私有部署候选：跟踪的服务源码、五个实际包、锁文件、已验证的项目运行／Link 镜像、准确提交及逐文件摘要；Linux 候选 CI 35973201941 验证并上传成功。支持加载后的不可变本地镜像 ID，未发布 registry 包／镜像，不能替代真实服务商与目标部署验收。
- [x] 最新主仓 `520d8b2a` 与 services `956f4932` 的私有部署候选通过 Linux CI 36224563408：五个实际包、独立安装、双项目容器、Link、归档与镜像加载；artifact 10899924126。它不包含 TUI 管理命令，未发布 registry 包／镜像，不能替代目标部署。
- [x] 服务包提供独立 `code-shell-settings-recovery`，不依赖 TUI；五包移位安装、只读运行镜像及实际受管项目卷验证通过。停止项目后按原挂载路径修复／逐字节回滚，再重启确认文件与会话保留；最终候选 CI 36226281163（Host `eb624983`、services `956f4932`）通过。PR #17 最终完整 CI 通过并合入 main `ca9895ed`，未公开发布。
- [x] 三仓私有候选纳入准确 Panel 提交及六个安装包；候选 Host 安装预检、完整清单／内容摘要、导出复验及镜像加载通过 Linux CI 36241763436，artifact 10906535658。Host PR #18／services PR #3 已合入；这是包与部署候选验证，不代表六个 Panel 的全部实际业务验收。
- [ ] 正式包／镜像发布流水线与三仓兼容交付；候选验证与 CI 通过不代表正式发布或部署完成。
- [ ] 发布包含本任务 Host 能力的兼容包，更新服务仓库固定版本；当前公开依赖仍是 0.9.22。
- [ ] 分开固定控制服务、Link、项目运行镜像的版本／摘要。
- [ ] 公网管理员初始化、域名、TLS、健康检查、日志、数据目录、资源限制。
- [ ] 真实模型、程序与 Panel 依赖配置；GitHub 真实凭据配置。
- [ ] 云端项目创建／启停／重启／持久化在目标服务器复验。
- [x] Cloud 控制目录与项目卷离线备份、新安装恢复；真实 Docker 验证恢复后项目启动、旧会话撤销及失败清理。Link 备份／恢复已有独立验证。
- [ ] 目标服务器的控制数据、项目卷、Link 数据恢复；跨版本升级和回滚演练。
- [ ] 清洁提交构建与发布记录，部署冒烟、回滚条件、数据迁移说明。
- [ ] 处理已记录的 Bun Hub WebSocket 测试崩溃，补齐回归结论。

真实上线需要：目标服务器、Cloud/Link 域名、GitHub Client ID 与准确回调地址。密钥写入服务器私密配置，不在聊天中传递。

## 实施顺序与发布门槛

1. 收尾主仓完整 CI；A 项目路由 + B 公共任务／资源能力优先验收电脑本地与云端业务。
2. C 项目版本绑定、数据迁移和升级回滚；将 B 的恢复和冲突处理扩展到其余 Panel。
3. D 六个 Panel 的桌面／云端核心流程，F 真实 Link 授权与真实模型／业务产物验收。
4. G 三仓兼容发布、固定依赖及镜像更新、目标部署与备份恢复／跨版本回滚。
5. E 统一设备目录／中继／通知单独交付；手机 Panel 触控、窄屏和物理设备操作验收后置，不作为前面核心流程收尾的前提，也不能提前标为完成。

每个组合检查：执行位置、真实输出、持久化、重复提交、断线恢复、权限撤销、版本更新。单元测试、模拟浏览器、真实 Docker、真实服务商、物理手机分别记录，不能互相代替。

不列入首版必要范围：本地／云端自动同步、跨主机任务迁移、公开多租户平台、多节点调度、不可信代码托管、完全离线执行、原生手机 App。
